pub mod janela_mensagem {
    /*  
        mod functions;
        // use crate::functions::janelas::janela_mensagem;
        // use crate::functions::sqlite3func::sqlite3_conn;
        // use crate::functions::janelas::janela_mensagem;

        fn main() { 

        // janela_mensagem::abrir_janela();
        // let mut f = File::open("cats.db")?; 
        // let mut f = File::open("foo.txt")?;
        // let mut conn = Connection::open("cats.db")?;
        // let mut f = File::open("cats.db")?;
        // let mut data = vec![];
        // f.read_to_end(&mut data)?;
        }
    */ 
    /*
    use orbtk::prelude::*; 
    pub fn abrir_janela(){
        orbtk::initialize();

        Application::new().window(|ctx| {
            Window::new()
                .title("Hello, OrbTk!")
                .position((100.0, 100.0))
                .size(200.0, 100.0)
                .child(
                    TextBlock::new()
                        .text("Hello, World gdfgdfgfg!")
                        .v_align("center")
                        .h_align("center")
                        .build(ctx)
                )
                .build(ctx)
        }).run();

    }
    */
}